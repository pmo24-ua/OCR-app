<?xml version="1.0" encoding="utf-8"?>
<!-- res/navigation/nav_graph.xml -->
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/nav_home">

    <!-- ─────────────────────────
         Fragment: Inicio (Home)
         ───────────────────────── -->
    <fragment
        android:id="@+id/nav_home"
        android:name="com.ejemplo.ocr.HomeFragment"
        android:label="Inicio"
        tools:layout="@layout/fragment_home">

        <!-- Acciones desde Home hacia otras pantallas
             (se usan en HomeFragment con findNavController()) -->

    </fragment>
    <fragment
        android:id="@+id/historyFragment"
        android:name="com.ejemplo.ocr.HistoryFragment"
        android:label="@string/historial"
        tools:layout="@layout/fragment_history"/>

    <fragment
        android:id="@+id/settingsFragment"
        android:name="com.ejemplo.ocr.SettingsFragment"
        android:label="Ajustes" />


</navigation>
